nasmのアセンブリで
フロッピーディスクのデータを再現している。

それをnasmでアセンブルし、qemuで起動する。

qemu-system-i386
-m 32        : このVMは32MBのメモリを詰む
-rtc         : base=localtime
-vga std     : グラフィックボードの指定 
-fda fdimage : フロッピードライブA fda にidimageというファイルを差し込んでから電源を入れろ。という指定


フロッピーディスクは1.44MBのデータ

昔のBIOSは、FAT16フォーマットのデータで最初の512バイトを読み込んで実行する。

BIOSは、フロッピーでもハードディスクでも、中身がwindowsでもlinuxでも一才気にせず、
ただ無心で、ディスクの物理的な一番先頭にあるセクタ(第一セクタ)だけをターゲットにする。
ディスクの世界では、このディスクのデータを読み書きする最小単位がセクタ。512バイト。と決まってる。

helloos.cは、512バイトをブートセクタ

フロッピーディスク
1セクタ=512バイト
1トイラック

フロッピーディスクの「1.44MB」というのは、歴史の中で最後に天下を撮った一番有名な規格の容量にすぎない。

## BIOSは、ディスクから拾い上げた512バイトのデータは、パソコンのメモリの0x700という固定アドレスにコピーする。
この0xx700というアドレスは、IBMのPCから伝説のアドレスとなっている。

## BIOSは、512バイトのうち、最後の2バイトをみにいく。
ここが、0x55, 0xAAというマジックナンバー(ブートシグニチャ)になっていれば「これは起動できるディスクだな」となる。

## それからBIOSは,ブートシグニチャを確認したら 0x7C00へジャンプさせてロードした512バイトの命令の実行に映る。

アセンブラ・ディレクティブ
.byte         : 1バイトのデータをそのままおけ。
.word         : 2バイトのデータをおけ(自動でリトルエンディアンにしてくれる)
.int          : 4バイトのデータをおけ。
.ascii        : 文字データをおけ。
.skip N, 0    : Nバイト分、0で埋めて飛ばせ。
.org アドレス : ファイルの先頭から、指定したアドレスまで一気にスキップしろ。

なんで,
.text
.data
.globl
がないのか。

結論、今回書いているプログラムはLinuxとかWindowsといったOSに読み込んでもらうためのものではないから。

普通のELF形式
リンカに細かく伝えるため, .text, .data, .globlとかをつける。

今回は、BIOSから実行する。
とりあえず、先頭から512バイトをメモリの0x7C00にドカンとおいて先頭から無条件に実行する。という力技で動く。


bootセクタを0x7cC00においたとして、fat形式のデータなので。
頭の3バイトに。

0xeb, 0x4e :JMP 78 78バイトジャンプする
0x90: nop : 何もしないパディング

とあり実際の命令の一までジャンプする。

